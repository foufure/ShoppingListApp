@using ShoppingListApp.Domain.Entities
@model IEnumerable<Article>

<div class="container-fluid">
    <div class="row">
        <span class="btn btn-primary navbar-btn btn-lg text-uppercase">@ShoppingListApp.i18n.Resources.Views.Article.ArticlesCommon.Articles</span>
    </div>

    <div class="row">
        <table class="table">
            <tbody>
                @foreach (Article article in @Model)
                {
                    <tr>
                        <td class="">
                            @Html.ActionLink(@article.ArticleName.ToString(), "ModifyArticle", "Article", new { articleToModifyID = article.ArticleID }, new { @class = "btn btn-primary" })
                        </td>
                        <td class="">
                            @using (@Html.BeginForm("RemoveArticle", "Article"))
                            {
                                @Html.Hidden("articleToRemoveID", article.ArticleID)
                                <button typeof="submit" class="btn btn-danger">@ShoppingListApp.i18n.Resources.Views.Article.ArticlesCommon.Delete</button>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
        
    <div class="row">
        @using (@Html.BeginForm("AddNewArticle", "Article"))
        { 
            @Html.TextBox("newArticleName", @ShoppingListApp.i18n.Resources.Views.Article.ArticlesCommon.YourArticleName, new { @autofocus = "autofocus" })
            <button typeof="submit" class="btn btn-success">@ShoppingListApp.i18n.Resources.Views.Article.ArticlesCommon.AddNewArticle</button>
        }
    </div>

    @Html.Partial("BackToMainMenu")
</div>


